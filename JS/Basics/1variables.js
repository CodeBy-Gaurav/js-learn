const accountId = 1234
let nameType = "employee"
let phoneNo = "indian number"
var system = "elvish bhaiii"

console.log(accountId)
console.table([accountId,nameType,phoneNo,system])

//we usde const keyword to declare constant variable 
//let used to declare a variable 
//var is also used to declare variables but we do nt use that beacuse it creates errors